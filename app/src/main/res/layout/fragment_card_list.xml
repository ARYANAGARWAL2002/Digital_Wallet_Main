<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/gray_light"
    tools:context=".fragments.CardListFragment">

    <!-- Spinner for category filtering with improved visibility -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        app:cardCornerRadius="8dp"
        app:cardElevation="6dp"
        android:background="@color/white">

        <Spinner
            android:id="@+id/spinner_category"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:textAlignment="center"
            android:textSize="16sp"
            android:popupBackground="@color/gray_dark" />
    </androidx.cardview.widget.CardView>



    <!-- RecyclerView for displaying cards -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_cards"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:padding="8dp"
        tools:listitem="@layout/item_card"
        android:background="@android:color/transparent" />

    <!-- TextView for no cards message -->
    <TextView
        android:id="@+id/text_view_no_cards"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="16dp"
        android:text="No cards found in this category"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/gray_dark"
        android:visibility="gone"
        tools:visibility="visible" />

</LinearLayout>
